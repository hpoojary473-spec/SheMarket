# Client Components

This frontend is plain HTML/CSS/JavaScript, so repeated UI such as the topbar and sidebars is written directly in each page for hackathon simplicity.

If the project later moves to React, this folder can hold reusable components such as:

- `Topbar`
- `SellerSidebar`
- `AdminSidebar`
- `ProductCard`
- `StatCard`
